using SwissTimingDisplay.Models;
using SwissTimingDisplay.ViewModels;
using System;
using System.ComponentModel;
using System.Diagnostics;
using System.Diagnostics.Metrics;
using System.Linq;
using System.Text;
using System.Windows;
using System.Windows.Controls;
using System.Windows.Media;
using System.Windows.Threading;
using static SwissTimingDisplay.ViewModels.MainViewModel;

namespace SwissTimingDisplay
{
    /// <summary>
    /// Interaction logic for SiriccoWindow.xaml
    /// Nb: Generated by copying WindGaugeWindow
    /// Interpretation of incoming data is different (to be defined)
    /// </summary>
    public enum LoopCountState
    {
        Stopped = -1,
        SignalStartSending = -2,
        SignalStopTimer = -3,
        InitialCountValue = 0,
        CountPerSecDefault = 4,
        AcquisitionDurationSecsDefault = 10
    }

    public partial class SiriccoWindow : Window
    {
        private MainWindow? _mainWindow;
        private readonly MainViewModel _vm;

        private readonly DispatcherTimer _WindGaugeTimer;
        private readonly Stopwatch _raceStopwatch = new Stopwatch();
        private bool _SiriccoIsRunning = false;
        private TimeSpan _raceElapsed = TimeSpan.Zero;
        private bool _sendWallClockWhileRunning = false;
        private bool _SiriccoHasStartedSinceReset = false;
        private bool _autoSendInProgress = false;

        private bool _isClosing = false;

        public Action? OnMaxLoopsExceeded;
        private readonly object _loopCountLock = new object();
        private int _loopCount = (int)LoopCountState.Stopped;
        private readonly object _loopCountLock2 = new object();
        private int _loopCount2 = (int)LoopCountState.Stopped;
        private double speedTally { get; set; } = 0;

        private TimeSpan SiriccoWindGaugePeriod { get; set; } = TimeSpan.FromSeconds(1 / (int)LoopCountState.CountPerSecDefault);
        int MaxLoops { get; set; } = WindGauge.SiriccoAcquisitionMeasurementsPerSecDefault * WindGauge.SirricoAcquisitionDurationSecsDefault; // Add a few extra ticks as buffer


        private bool SetupSimulatedWindGaugeTimerForNewStart()
        {
            if (_WindGaugeTimer == null)
            {
                Debug.WriteLine("WindGauge timer is not initialized.");
                return false;
            }

            int currentCount = GetLoopCount();
            if (currentCount >= (int)LoopCountState.InitialCountValue)
            {
                Debug.WriteLine("Cannot change acquisition parameters while timer is running.");
                MessageBox.Show("Cannot change acquisition parameters while timer is running. Please stop the timer before changing parameters.", "Parameter Change Not Allowed", MessageBoxButton.OK, MessageBoxImage.Warning);
                return false;
            }
            SiriccoWindGaugePeriod = TimeSpan.FromSeconds(1.0 / WindGauge.SiriccoWindGaugeAcquisitionDurationSecs);
            _WindGaugeTimer.Interval = SiriccoWindGaugePeriod;
            MaxLoops = WindGauge.SiriccoWindGaugeCaptureCountsPerSec * WindGauge.SiriccoWindGaugeAcquisitionDurationSecs;
            return true;
        }

        private bool  SetupSimulatedWindGaugeTimerInitial()
        {
            SetLoopCount((int)LoopCountState.Stopped);
            WindGauge.SiriccoWindGaugeAcquisitionDurationSecs = (int)LoopCountState.AcquisitionDurationSecsDefault;
            WindGauge.SiriccoWindGaugeCaptureCountsPerSec = (int)LoopCountState.CountPerSecDefault;
            return SetupSimulatedWindGaugeTimerForNewStart();
        }

        /// <summary>
        /// Sets up and starts the simulated WindGauge timer with default parameters. 
        /// The timer will send speed data at the configured rate.
        /// It will automatically stop counting when max value is reached and signal thus
        ///    ... but will not stop sending data until the timer is stopped.
        /// If the timer is already running, it will not be restarted and a warning will be shown. 
        /// If an attempt to change the acquisition parameters while the timer is counting, they are ignored.
        /// </summary>
        public void WindGaugeStart()
        {     
            if (_WindGaugeTimer == null)
            {
                Debug.WriteLine("WindGauge timer is not initialized.");
                return;
            }
            if (_WindGaugeTimer.IsEnabled)
            {
                Debug.WriteLine("WindGauge timer is already running. Restart is not allowed.");
                MessageBox.Show("WindGauge timer is already running. Please stop the timer before restarting.", "Restart Not Allowed", MessageBoxButton.OK, MessageBoxImage.Warning);
                return;
            }
            if (!SetupSimulatedWindGaugeTimerForNewStart())
            {
                Debug.WriteLine("Failed to set up WindGauge timer for initial start.");
                return;
            }
            SetLoopCount((int)LoopCountState.SignalStartSending);
            _WindGaugeTimer.Start();    
        }

        /// <summary>
        /// This signals the simulated WindGauge timer to stop.
        /// </summary>
        public void WindGaugeStop()
        {
            SetLoopCount((int)LoopCountState.SignalStopTimer);
            _WindGaugeTimer.Stop();
        }


        private int GetLoopCount()
        {
            lock (_loopCountLock)
            {
                return _loopCount;
            }
        }

        private void SetLoopCount(int value)
        {
            lock (_loopCountLock)
            {
                _loopCount = value;
            }
        }

        private int GetLoopCount2()
        {
            lock (_loopCountLock2)
            {
                return _loopCount2;
            }
        }

        private void ClearLoopCount2()
        {
            lock (_loopCountLock2)
            {
                _loopCount2 = 0;
            }
        }
        private void SetLoopCount2(int value)
        {
            lock (_loopCountLock2)
            {
                _loopCount2 = value;
            }
        }

        private int IncrementLoopCount2()
        {
            lock (_loopCountLock2)
            {
                return ++_loopCount2;
            }
        }

        public SiriccoWindow() : this(MainViewModel.SharedInstance, null)
        {
        }

        public SiriccoWindow(MainViewModel vm, MainWindow mainWindow = null)
        {
            _vm = vm;
            _mainWindow = mainWindow;
            _vm.WindGaugeStart = WindGaugeStart;
            _vm.WindGaugeStop = WindGaugeStop;
            InitializeComponent();
            DataContext = _vm;
            if(!string.IsNullOrEmpty(_vm?.SelectedReceivePortName))
                _vm.ConnectReceive(_vm.SelectedReceivePortName);

            _vm.PropertyChanged += VmOnPropertyChanged;
            _vm.SiriccoDataReceived += VmOnSiriccoDataReceived;

            Loaded += (_, _) => ApplyAnchorLayout();
            SizeChanged += (_, _) => ApplyAnchorLayout();

            _WindGaugeTimer = new DispatcherTimer
            {
                Interval = SiriccoWindGaugePeriod
            };
            SetupSimulatedWindGaugeTimerInitial();
            _WindGaugeTimer.Tick += async (_, _) =>
            {
                byte[] cmdbytes = new byte[0];

                // Generate the wind speed data message.
                double WindSpeed = new Random().Next(-100, 100); // Simulate wind speed measurement
                WindSpeed /= 10.0;
                int uVector = 0;
                if (WindSpeed < 0)
                    uVector = 1;
                string speedStr = WindSpeed >= 0 ? $"+{WindSpeed:0.0}" : $"{WindSpeed:0.0}";
                // Could do a switch here for the different Gill anemometers
                string msg = $"Q,{speedStr},{uVector},00,M,";
                var msgBytes = msg.Select(c => (byte)c).ToArray();

                // Get Checksum:
                // cs++; <-- Was done as below to invoke a failure in the Gill WinView application
                // It was oberved that the app does verify the checksum as it failed with that included
                // ... but does decode the messages when not inserted above.
                // ... and so the format of its inclusion here is correct.
                byte cs = 0;
                foreach (char c in msg)
                {
                    cs ^= (byte)c;
                }
                //cs++; See above comment on checksum verification in Gill WinView app
                byte[] csBytes = Encoding.ASCII.GetBytes(cs.ToString());
                //.Resize(ref csBytes, csBytes.Length + 1);

                // Start building the command bytes array with the correct size: STX + data + ETX + checksum + CR + LF
                cmdbytes = new byte[msgBytes.Length + csBytes.Length + 4];

                // Starts with STX
                cmdbytes[0] = (byte)CharCommand.STX;

                // Insert data bytes
                Array.Copy(msgBytes, 0, cmdbytes, 1, msgBytes.Length);

                // Ends with ETX
                cmdbytes[msgBytes.Length + 1] = (byte)CharCommand.ETX;

                // Append Checksum
                Array.Copy(csBytes, 0, cmdbytes, msgBytes.Length + 2, csBytes.Length);

                // Append CR, LF
                cmdbytes[cmdbytes.Length - 2] = (byte)CharCommand.CR;
                cmdbytes[cmdbytes.Length - 1] = (byte)CharCommand.LF;
                var payload = cmdbytes;
                await BeginAutoSend(payload);
            };


            //UpdateSiriccoStartButtonContent();
            //UpdateWallClockEnabledState();
            //UpdateNumDigitsEnabledState();
            //UpdateSendEnabledState();
            //UpdateRaceTimerEnabledState();
            //UpdateLapContinueButton();
        }

        private void VmOnPropertyChanged(object? sender, PropertyChangedEventArgs e)
        {
            if (e.PropertyName == nameof(MainViewModel.IsConnected)
                || e.PropertyName == nameof(MainViewModel.SelectedTcpCommand))
            {
                UpdateRaceTimerEnabledState();
                UpdateSendEnabledState();
            }

            if (e.PropertyName == nameof(MainViewModel.AnchorDisplay))
            {
                ApplyAnchorLayout();
            }
        }

        private void ApplyAnchorLayout()
        {
            if (rightDisplayColumn is null
                || rightSevenSegBorder is null
                || rightSevenSegViewbox is null
                || rightSevenSegContent is null
                || bottomSevenSegBorder is null
                || bottomSevenSegViewbox is null
                || bottomSevenSegContent is null)
            {
                return;
            }

            if (!_vm.AnchorDisplay)
            {
                rightDisplayColumn.Width = new GridLength(0);
                rightSevenSegBorder.Visibility = Visibility.Collapsed;

                bottomSevenSegBorder.Visibility = Visibility.Visible;
                bottomSevenSegBorder.HorizontalAlignment = HorizontalAlignment.Left;
                bottomSevenSegBorder.VerticalAlignment = VerticalAlignment.Top;
                bottomSevenSegViewbox.HorizontalAlignment = HorizontalAlignment.Left;
                bottomSevenSegViewbox.VerticalAlignment = VerticalAlignment.Center;
                bottomSevenSegViewbox.Stretch = Stretch.None;
                bottomSevenSegViewbox.StretchDirection = StretchDirection.Both;
                return;
            }

            Dispatcher.BeginInvoke(() =>
            {
                if (!_vm.AnchorDisplay)
                {
                    return;
                }

                var scaleRight = EvaluateRightAnchorScale();
                var scaleBottom = EvaluateBottomAnchorScale();

                if (scaleRight >= scaleBottom)
                {
                    ShowRightAnchored();
                }
                else
                {
                    ShowBottomAnchored();
                }
            }, DispatcherPriority.Loaded);
        }

        private double EvaluateRightAnchorScale()
        {
            ShowRightAnchored();
            UpdateLayout();
            return ComputeUniformScale(rightSevenSegViewbox, rightSevenSegContent);
        }

        private double EvaluateBottomAnchorScale()
        {
            ShowBottomAnchored();
            UpdateLayout();
            return ComputeUniformScale(bottomSevenSegViewbox, bottomSevenSegContent);
        }

        private void ShowRightAnchored()
        {
            rightDisplayColumn.Width = GridLength.Auto;
            rightSevenSegBorder.Visibility = Visibility.Visible;
            rightSevenSegViewbox.Stretch = Stretch.Uniform;
            rightSevenSegViewbox.StretchDirection = StretchDirection.Both;

            bottomSevenSegBorder.Visibility = Visibility.Collapsed;
        }

        private void ShowBottomAnchored()
        {
            rightDisplayColumn.Width = new GridLength(0);
            rightSevenSegBorder.Visibility = Visibility.Collapsed;

            bottomSevenSegBorder.Visibility = Visibility.Visible;
            bottomSevenSegBorder.HorizontalAlignment = HorizontalAlignment.Stretch;
            bottomSevenSegBorder.VerticalAlignment = VerticalAlignment.Top;
            bottomSevenSegViewbox.HorizontalAlignment = HorizontalAlignment.Center;
            bottomSevenSegViewbox.VerticalAlignment = VerticalAlignment.Top;
            bottomSevenSegViewbox.Stretch = Stretch.Uniform;
            bottomSevenSegViewbox.StretchDirection = StretchDirection.Both;
        }

        private static double ComputeUniformScale(Viewbox viewbox, FrameworkElement child)
        {
            if (viewbox.ActualWidth <= 0 || viewbox.ActualHeight <= 0)
            {
                return 0;
            }

            child.Measure(new Size(double.PositiveInfinity, double.PositiveInfinity));
            var desired = child.DesiredSize;
            if (desired.Width <= 0 || desired.Height <= 0)
            {
                return 0;
            }

            return Math.Min(viewbox.ActualWidth / desired.Width, viewbox.ActualHeight / desired.Height);
        }

        private void UseWallClock_Checked(object sender, RoutedEventArgs e)
        {
            UpdateWallClockEnabledState();
            UpdateNumDigitsEnabledState();
            UpdateSendEnabledState();
        }

        private void UseWallClock_Unchecked(object sender, RoutedEventArgs e)
        {
            UpdateWallClockEnabledState();
            UpdateNumDigitsEnabledState();
            UpdateSendEnabledState();
        }

        private void SiriccoMessageMode_SelectionChanged(object sender, System.Windows.Controls.SelectionChangedEventArgs e)
        {
            if (e.AddedItems.Count > 0 && e.AddedItems[0] is SiriccoMessageModes mode)
            {
                if (mode == SiriccoMessageModes.WindGauge_ReadFrequency)
                {
                    Debug.WriteLine("WindGauge_ReadFrequency mode selected");
                    // Add logic for WindGauge_ReadFrequency mode here
                }
            }
        }

        private void VmOnSiriccoDataReceived(SiriccoData.SiriccoResult? result)
        {
            if (result == null)
            {
                return;
            }
            else if ( !result.IsValid)
            {
                Debug.WriteLine($"Invalid Siricco data received: {result?.ErrorMessage}");
                return;
            }
            // Valid Siricco message received
            
            // TODO: Add interpretation logic here when defined
            int count = IncrementLoopCount2();
            double speedval = result.Speed;
            switch(result.Mode)
            {
                case SiriccoMessageModes.Gill_Tunnel:
                    speedval = result.Speed;
                    break;
                case SiriccoMessageModes.Gill_PolarContinuous:
                    speedval = result.Speed1;
                    int direction = result.Direction;
                    break;
                default:
                    break;
            }
            speedTally += speedval;
            Debug.WriteLine($"Added to tally: {speedval}, Current tally: {speedTally}, Count: {count} Mode:{ result.Mode}");
            if (count>= MaxLoops)
            {               
                //double speed = speedTally /count;
                double speed = Math.Round(speedTally / count, 1);
                _vm.RecvStatus = $"Speed={speed:F1} {speed:F3} (final,Total: {speedTally} over {count} measurements averaged over {WindGauge.SiriccoWindGaugeAcquisitionDurationSecs} sec )";
                Debug.WriteLine($"{_vm.RecvStatus}");
                WindGauge.WindSpeed = speed;
                Siricco_StartButton(null, null);
            }
            else
            {
                _vm.RecvStatus = $"Speed={result.Speed1}";
            }
        }

        protected override void OnClosed(EventArgs e)
        {
            _isClosing = true;
            _WindGaugeTimer.Stop();
            _vm.PropertyChanged -= VmOnPropertyChanged;
            _vm.SiriccoDataReceived -= VmOnSiriccoDataReceived;
            // Don't dispose _vm since it's shared
            base.OnClosed(e);
        }

        private async void Siricco_StartButton(object sender, RoutedEventArgs e)
        {
            if (_SiriccoIsRunning)
            {
                _SiriccoIsRunning = false;
                _vm.SiriccoIsRunning = false;
                _vm.IsRaceRunning = false;
                //_raceStopwatch.Stop();

                //_WindGaugeTimer.Stop();
                _vm.DisplaySimulatorSpeed = true;
                _vm.UpdateSimulatedWindGaugeDisplaySpeed();

                _sendWallClockWhileRunning = false;


                UpdateTimeInputFromRaceElapsed();
                _vm.Status = "Race timer stopped.";

                UpdateSiriccoStartButtonContent();
                UpdateWallClockEnabledState();
                UpdateNumDigitsEnabledState();
                UpdateSendEnabledState();
                UpdateLapContinueButton();

                return;
            }

            if (_SiriccoHasStartedSinceReset)
            {
                //SendCmd(TcpCommand.WindGauge_Reset_Stop_Clear);
                speedTally = 0.0;
                _raceStopwatch.Reset();
                _raceElapsed = TimeSpan.Zero;
                _sendWallClockWhileRunning = false;
                _SiriccoHasStartedSinceReset = false;
                _vm.RaceHasStartedSinceReset = false;
                _vm.IsRaceRunning = false;

                // Send clear command to display
                if (_vm.IsConnected)
                {
                    try
                    {
                        var clearCommand = TcpCommandDefinitions.GetPayloadBytes(TcpCommand.Roller_Time_Mode_Clear).ToArray();
                        await _vm.SendRawAsync(clearCommand);
                        _vm.Status = $"Sent {clearCommand.Length} byte(s) to {_vm.ConnectedPortName}.";
                    }
                    catch (Exception ex)
                    {
                        _vm.Status = ex.Message;
                    }
                }

                _vm.TimeInput = string.Empty;
                UpdateTimeInputFromRaceElapsed();
                _vm.Status = "Race timer reset.";

                UpdateSiriccoStartButtonContent();
                UpdateWallClockEnabledState();
                UpdateNumDigitsEnabledState();
                UpdateSendEnabledState();
                UpdateLapContinueButton();

                return;
            }

            _SiriccoIsRunning = true;
            _vm.SiriccoIsRunning = true;
            _vm.IsRaceRunning = true;

            ClearLoopCount2();

            int period = _vm.WindGaugeCaptureCountdownPeriodSecs;
            int cps = _vm.WindGaugeCaptureCountsPerSec;
            MaxLoops = period * cps;
            speedTally = 0.0;

            _sendWallClockWhileRunning = _vm.UseWallClockTimeOfDay;
            _SiriccoHasStartedSinceReset = true;
            _vm.RaceHasStartedSinceReset = true;

            _vm.StartCountDown();

            UpdateTimeInputFromRaceElapsed();
            _vm.Status = "Siricco Wind Gauge capture started.";

            UpdateSiriccoStartButtonContent();
            UpdateWallClockEnabledState();
            UpdateNumDigitsEnabledState();
            UpdateSendEnabledState();
            UpdateLapContinueButton();
        }


        private void ResendLastButton_Click(object sender, RoutedEventArgs e)
        {

            SendCmd(TcpCommand.WindGauge_Resend_Latest);
            ForceUiUpdate();
            UpdateSiriccoStartButtonContent();
            UpdateLapContinueButton();
        }

        private void UpdateSiriccoStartButtonContent()
        {
            if (btnSiriccoStart is null)
            {
                return;
            }

            if (_SiriccoIsRunning)
            {
                btnSiriccoStart.Content = "Stop";
                return;
            }

            btnSiriccoStart.Content = _SiriccoHasStartedSinceReset ? "Reset" : "Start";
        }

        private void UpdateLapContinueButton()
        {
            // Resend Last button should always be visible
            if (btnResendLast is not null)
            {
                btnResendLast.Visibility = Visibility.Visible;
                btnResendLast.IsEnabled = !_SiriccoIsRunning;
            }
        }

        private void ForceUiUpdate()
        {
            // Force immediate UI rendering with high priority
            Dispatcher.Invoke(() => { }, DispatcherPriority.Render);
        }

        private void UpdateWallClockEnabledState()
        {
            if (cbUseWallClock is null)
            {
                return;
            }

            if (btnSiriccoStart?.Content is not string content)
            {
                cbUseWallClock.IsEnabled = false;
                return;
            }

            // Enabled only when race timer button is showing "Start".
            cbUseWallClock.IsEnabled = content == "Start";
        }

        private void UpdateNumDigitsEnabledState()
        {
            if (cbNumDigits is null)
            {
                return;
            }

            cbNumDigits.IsEnabled = !_SiriccoIsRunning && !_SiriccoHasStartedSinceReset;
        }

        private void UpdateSendEnabledState()
        {
            if (btnSend is null)
            {
                return;
            }

            // Disable Send after Start has been pressed until Reset returns to Start state.
            btnSend.IsEnabled = true;// _vm.IsConnected && !_raceIsRunning && !_raceHasStartedSinceReset;
        }

        private void UpdateRaceTimerEnabledState()
        {
            if (btnSiriccoStart is null)
            {
                return;
            }

            var enabled = _vm.IsConnected;
            btnSiriccoStart.IsEnabled = enabled;

            if (!enabled && (_SiriccoIsRunning || _SiriccoHasStartedSinceReset))
            {
                ResetRaceTimerState();
            }
        }

        private async void ResetRaceTimerState()
        {
            _SiriccoIsRunning = false;
            _vm.SiriccoIsRunning = false;
            _vm.IsRaceRunning = false;
            _raceStopwatch.Reset();
            _raceElapsed = TimeSpan.Zero;
            _sendWallClockWhileRunning = false;
            _SiriccoHasStartedSinceReset = false;
            _vm.RaceHasStartedSinceReset = false;

            // Send clear command to display
            if (_vm.IsConnected)
            {
                try
                {
                    var clearCommand = TcpCommandDefinitions.GetPayloadBytes(TcpCommand.Roller_Time_Mode_Clear).ToArray();
                    await _vm.SendRawAsync(clearCommand);
                    _vm.Status = $"Sent {clearCommand.Length} byte(s) to {_vm.ConnectedPortName}.";
                }
                catch (Exception ex)
                {
                    _vm.Status = ex.Message;
                }
            }

            _vm.TimeInput = string.Empty;
            UpdateTimeInputFromRaceElapsed();

            UpdateSiriccoStartButtonContent();
            UpdateWallClockEnabledState();
            UpdateNumDigitsEnabledState();
            UpdateSendEnabledState();
            UpdateLapContinueButton();
        }

        private async Task BeginAutoSend(byte[] cmdbytes)
        {
            if (_autoSendInProgress || _isClosing)
            {
                return;
            }

            _autoSendInProgress = true;
            try
            {
                if (!_isClosing)
                {
                    await _vm.SendRawAsync(cmdbytes);
                    //_vm.SendStatus = $"Sent {cmdbytes.Length} byte(s) {cmdbytes} to {portName}.";
                    string csv = string.Join("_", cmdbytes.Select(b => MainViewModel.CharCommandToString((CharCommand)b)));
                    csv = csv.Replace(" ", "<SPC>");
                    csv = csv.Replace("_", "");

                    _vm.SendStatus = $"Sent:\t{csv}";
                }
            }
            catch (Exception ex)
            {
                if (!_isClosing)
                {
                    _vm.Status = ex.Message;
                }
            }
            finally
            {
                _autoSendInProgress = false;
            }
        }

        private void UpdateTimeInputFromRaceElapsed()
        {
            var hours = (int)_raceElapsed.TotalHours;
            var minutes = _raceElapsed.Minutes;
            var seconds = _raceElapsed.Seconds;
            var tenths = _raceElapsed.Milliseconds / 100;
            var hundredths = (_raceElapsed.Milliseconds / 10) % 100;

            if (hours > 0)
            {
                if (hours > 9)
                {
                    hours = 9;
                }

                _vm.TimeInput = $"{hours}:{minutes:00}:{seconds:00}.{tenths}";
                return;
            }

            var totalMinutes = (int)_raceElapsed.TotalMinutes;
            if (totalMinutes > 99)
            {
                totalMinutes = 99;
            }

            _vm.TimeInput = $"{totalMinutes:00}:{seconds:00}.{hundredths:00}";
        }

        private byte[] BuildExpandedPayload(string cmd)
        {
            var xx =  cmd.Select(c => (byte)c).ToArray();
            var x2 = new byte[cmd.Length];
            int i = 0;
            foreach (char c in cmd)
            {
                byte b = (byte)c;
                x2[i++] = b;
                
            }

            return xx;
        }

        private async void SendCmd(TcpCommand cmd)
        {
            if (cmd == TcpCommand.WindGauge_Acquisition_Duration)
            {
                //CharCommand[] cmdx;
                //byte[] cmdbytes = null;
                //if (TcpCommandDefinitions.Commands.TryGetValue(cmd, out var charCommands))
                //{
                //    cmdx = charCommands.ToArray<CharCommand>();
                //    cmdbytes = cmdx.Select(c => (byte)c).ToArray();

                //    if (_vm.IsConnected)
                //    {
                //        var portName = _vm.ConnectedPortName ?? "(unknown)";
                //        var send = MessageBoxResult.Yes;

                //        if (send == MessageBoxResult.Yes)
                //        {
                //            await _vm.SendRawAsync(cmdbytes);
                //            _vm.Status = $"Sent {cmdbytes.Length} byte(s) to {portName}.";


                //        }
                //    }
                //}
            }
            else if (cmd == TcpCommand.WindGauge_Reset_Stop_Clear)
            {
                CharCommand[] cmdx;
                byte[] cmdbytes = null;
                if (TcpCommandDefinitions.Commands.TryGetValue(cmd, out var charCommands))
                {
                    cmdx = charCommands.ToArray<CharCommand>();
                    cmdbytes = cmdx.Select(c => (byte)c).ToArray();

                    if (_vm.IsConnected)
                    {
                        var portName = _vm.ConnectedPortName ?? "(unknown)";
                        var send = MessageBoxResult.Yes;

                        if (send == MessageBoxResult.Yes)
                        {
                            await _vm.SendRawAsync(cmdbytes);
                            _vm.Status = $"Sent {cmdbytes.Length} byte(s) to {portName}.";


                        }
                    }
                }
            }
            else
            {
                byte[] cmdbytes = null;
                string msg = " Q,001.59,1,00,M,";
                byte cs = 0;
                foreach (char c in msg)
                {
                    cs ^= (byte)c;
                }

                var msgBytes = msg.Select(c => (byte)c).ToArray();
                var csBytes = Encoding.ASCII.GetBytes(cs.ToString());
                cmdbytes = new byte[msgBytes.Length + csBytes.Length + 4];
                cmdbytes[0] = (byte)CharCommand.STX;
                Array.Copy(msgBytes, 0, cmdbytes, 1, msgBytes.Length);
                cmdbytes[msgBytes.Length + 1] = (byte)CharCommand.ETX;
                cmdbytes[msgBytes.Length + 2] = cs;
                cmdbytes[msgBytes.Length + 3] = (byte)CharCommand.CR;
                cmdbytes[msgBytes.Length + 4] = (byte)CharCommand.LF;
                var payload = cmdbytes;

                if (_vm.IsConnected)
                {
                    var portName = _vm.ConnectedPortName ?? "(unknown)";
                    var send = MessageBoxResult.Yes;

                    if (send == MessageBoxResult.Yes)
                    {
                        await _vm.SendRawAsync(cmdbytes);
                        //_vm.SendStatus = $"Sent {cmdbytes.Length} byte(s) {cmdbytes} to {portName}.";
                        string csv = string.Join("_", cmdbytes.Select(b => MainViewModel.CharCommandToString((CharCommand)b)));
                        csv = csv.Replace(" ", "<SPC>");
                        csv = csv.Replace("_", "");

                        _vm.SendStatus = $"  Sent: {csv}";
                    }
                }
            }
        }

        private async void ButtonSend_Click(object sender, RoutedEventArgs e)
        {
            // Check if SiriccoMessageMode is WindGauge_ReadFrequency and handle it
            //if (_vm.SiriccoMessageMode == SiriccoMessageModes.WindGauge_ReadFrequency)
            //{
            //    HandleWindGaugeReadFrequencySend();
            //    return;
            //}
            if (string.IsNullOrWhiteSpace(_vm.WindGaugeCaptureCountdownPeriodSecsStr))
            {
                _vm.WindGaugeCaptureCountdownPeriodSecsStr = ((int)LoopCountState.AcquisitionDurationSecsDefault).ToString(); ;
            }
            if (string.IsNullOrWhiteSpace(_vm.WindGaugeCaptureCountsPerSecStr))
            {
                _vm.WindGaugeCaptureCountsPerSecStr = WindGauge.SiriccoAcquisitionMeasurementsPerSecDefault.ToString();
            }
            var cmd = _vm.SelectedTcpCommand;
            // Clear TimeOut on send side if this was a Clear command
            if (cmd == TcpCommand.WindGauge_Reset_CaptureTime)
            {
                _vm.WindGaugeCaptureCountdownPeriodSecsStr = "10";
                return;
            }
            else if (cmd == TcpCommand.WindGauge_Acquisition_Duration)
            {
                if (int.TryParse(_vm.WindGaugeCaptureCountdownPeriodSecsStr, out int tim))
                {
                    if ((tim > 0) && (tim <= 100))
                    {
                        _vm.WindGaugeCaptureCountdownPeriodSecsStr = tim.ToString();
                        WindGauge.SiriccoWindGaugeAcquisitionDurationSecs = tim;
                    }
                }

            }
            else if (cmd == TcpCommand.WindGauge_ReadFrequency)
            {
                if (int.TryParse(_vm.WindGaugeCaptureCountsPerSecStr, out int counts))
                {
                    if ((counts > 1) && (counts <= 10))
                    {
                        _vm.WindGaugeCaptureCountsPerSecStr = counts.ToString();
                        WindGauge.SiriccoWindGaugeCaptureCountsPerSec = counts;
                    }
                }

            }
        }

        private async void HandleWindGaugeReadFrequencySend()
        {
            Debug.WriteLine("Handling WindGauge_ReadFrequency send action");
            // TODO: Implement the send logic for WindGauge_ReadFrequency
            // For example:
            // - Send the WindGauge_ReadFrequency TCP command
            // - Configure the serial port for frequency reading mode
            // - Update UI to indicate frequency reading mode is active
        }

        private async void SendClearButton_Click(object sender, RoutedEventArgs e)
        {
            if( _SiriccoIsRunning)
            {
                SendCmd(TcpCommand.WindGauge_Reset_Stop_Clear);

                _SiriccoIsRunning = false;
                _vm.SiriccoIsRunning = false;
                _vm.IsRaceRunning = false;
                _SiriccoHasStartedSinceReset = false;
                _vm.RaceHasStartedSinceReset = false;
                
                UpdateSiriccoStartButtonContent();
                UpdateWallClockEnabledState();
                UpdateNumDigitsEnabledState();
                UpdateSendEnabledState();
                UpdateLapContinueButton();
            }

        }

        private void BackToMainButton_Click(object sender, RoutedEventArgs e)
        {
            // Set current window and save state
            _vm.CurrentWindow = MainViewModel.ActiveWindow.Display;
            _vm.ShowWindGaugeWindow = false;

            if (_mainWindow != null)
            {
                _mainWindow.Show();
            }
            else
            {
                _mainWindow = new MainWindow(_vm);
                _mainWindow.Show();
            }
            this.Hide();
        }

        private void ExitButton_Click(object sender, RoutedEventArgs e)
        {
            _vm.BeginShutdown();
            Application.Current.Shutdown();
        }

        private void Window_Closing(object sender, System.ComponentModel.CancelEventArgs e)
        {
            _vm.BeginShutdown();
            Application.Current.Shutdown();
        }
    }
}
